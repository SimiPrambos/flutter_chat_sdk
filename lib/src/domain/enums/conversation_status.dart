/// Status of a conversation.
enum ConversationStatus {
  /// Conversation is active.
  active,

  /// Conversation is archived (hidden from main list).
  archived,

  /// Conversation is deleted.
  deleted,
}
